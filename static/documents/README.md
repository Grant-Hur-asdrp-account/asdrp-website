# Resume Placeholder

Drop your PDF resume in this folder (for example, `grant-hur-resume.pdf`) and
update `personal["resume_static_path"]` inside `portfolio_data.py` so the
download button on the Resume page points to the new file.
